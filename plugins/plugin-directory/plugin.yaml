id: plugin-directory
name: Plugin Directory
description: Curated extension catalog for Nexora and Backstage plugins

type: PLATFORM
lifecycle: ENABLED

frontend:
  package: "@internal/plugin-directory"
  route: "/plugin-directory"

backend:
  package: "@internal/plugin-directory-backend"
  route: "/api/plugin-directory"

owner: platform-team

permissions:
  - pluginDirectory.read
  - pluginDirectory.admin

validation:
  status: NOT_VALIDATED
  reference: plugin-directory-design.md
