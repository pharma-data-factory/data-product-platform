# Validation Expert runtime

This directory holds the v0.1 **approved development store** for Validation Expert runs (`runs-store.json`) and optional runtime evidence files.

- Do not treat this store as a validated Part 11 audit trail.
- Do not overwrite `validation/execution/evidence/` RC1/RC2 history from here.
- Product validation status remains **NOT_VALIDATED**.
