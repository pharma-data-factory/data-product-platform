[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "${{ values.name }}"
version = "${{ values.version }}"
description = "${{ values.description }}"
readme = "README.md"
requires-python = ">=3.12"
authors = [{ name = "Data Product Platform" }]
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.32.0",
  "pydantic>=2.9.0",
  "pydantic-settings>=2.6.0",
  "paho-mqtt>=2.1.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.3.0",
  "httpx>=0.27.0",
  "ruff>=0.7.0",
  "pip-audit>=2.7.0",
]

[tool.setuptools]
py-modules = ["app", "config", "logging_config", "mqtt_client"]
package-dir = {"" = "src"}

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
