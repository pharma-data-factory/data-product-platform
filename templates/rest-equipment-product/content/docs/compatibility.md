# Compatibility

Owner: Golden Path Team  
Template: rest-equipment-data-product

This product vendors Data Product SDK `1.x` and Standard `1.0.x`.
Contract `equipment-event` is versioned independently (`1.0.0`).

See `compat/consumers.json` and `compat/published.schema.json`.
Breaking contract changes require a MAJOR contract version.
