name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install
        run: pip install -e ".[dev]"

      - name: Lint
        run: ruff check app tests

      - name: Unit tests
        run: pytest

      - name: Build
        run: python -m compileall app

      - name: Security scan
        run: pip check

      - name: Docker build
        run: docker build -t ${{ values.name }}:ci .
