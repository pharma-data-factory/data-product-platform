# Canonical Unified Namespace source

The runnable Unified Namespace lives in `uns/`.

`templates/unified-namespace/content/` is the scaffolder snapshot used to
generate customer repositories. It is not a second independently evolved
implementation.

When UNS behavior changes, update `uns/` first, then refresh the template
content from that source. UNS remains DEVELOPMENT.
