# Release Notes

## 1.1.0

- `eventId` is required and unique.
- Ingest is idempotent on `eventId`.
- Data contract version is `1.1.0`.
- Quality endpoint evaluates mandatory checks.
- Compatibility tests gate breaking changes for `1.x` consumers.

## 1.0.0

- Initial MQTT temperature data product Golden Path.
