# Compatibility

Owner: Golden Path Team  
Template: mqtt-temperature-data-product

This product vendors Data Product SDK `1.x` and Standard `1.0.x`.
Contract `temperature-event` is versioned independently (`1.1.0`).

See `compat/consumers.json` and `compat/published.schema.json` in this
repository. Breaking contract changes require a MAJOR contract version.
