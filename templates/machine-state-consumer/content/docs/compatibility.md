# Compatibility

Uses the existing Data Product compatibility policy.

- `1.0.0` → `1.1.0` with an optional payload field is `COMPATIBLE`
- `1.x` → `2.0.0` with a breaking state contract is `BREAKING_CHANGE`

See `compat/published.schema.json` and `compat/consumers.json`.
