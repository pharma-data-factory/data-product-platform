# Python Microservice Template

Official Data Product Platform template for FastAPI services.

The generated repository includes source code, tests, Docker, GitHub Actions,
TechDocs, and a `catalog-info.yaml` that registers the service as a Data Product.
