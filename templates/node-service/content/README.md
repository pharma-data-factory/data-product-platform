# ${{ values.title }}

${{ values.description }}

Generated by the Data Product Platform **Node.js Microservice** template.

## Requirements

- Node.js 20+
- Docker (optional)

## Run locally

```bash
npm install
npm run dev
```

Health check:

```bash
curl http://localhost:8080/health
```

Expected:

```json
{
  "status": "UP",
  "service": "${{ values.name }}",
  "version": "${{ values.version }}"
}
```

## Test

```bash
npm test
```

## Lint and build

```bash
npm run lint
npm run build
```

## Docker

```bash
docker compose up --build
```

## CI

GitHub Actions runs lint, unit tests, TypeScript build, Docker build, and
`npm audit` on every pull request.

## Catalog

`catalog-info.yaml` registers this repository as a Data Product in Backstage.
