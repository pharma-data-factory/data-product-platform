# Local Development

Install Wave 1 packages from `vendor/`, then `pip install -e ".[dev]"`.
Start with `uvicorn app.main:app --port 8080`. MQTT is optional.
See [Event Simulator](event-simulator.md).
