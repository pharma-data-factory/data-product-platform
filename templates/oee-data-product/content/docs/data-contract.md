# Data Contract

Primary published contract: `oee-result` 1.0.0
(`contracts/oee-result.schema.json`).

Input contracts are listed in [Input Contracts](input-contracts.md).
Catalog `providesApis`: `${{ values.name }}--oee-result`.
